bot-token = ""

[vanilla-mc]
cmd-exec-dir = "F:\\Users\\photovoltex\\Code\\repos\\manager-bot-rs\\test\\process" # has to be a full path
cmd-path = "C:\\Program Files\\Java\\jdk-17.0.5\\bin\\java"
cmd-args = [ "-Xmx1024M", "-Xms1024M", "-jar", "server.jar", "nogui" ]
[vanilla-mc.startup]
msg = "???"
time-to-wait = 10 # time to wait after command execution, or stdout finish
wait-for-stdout = true # if true waits for the stdout to finish befor counting the time
# remainings are slash-commands
[vanilla-mc.slash-commands]
start = { description = "Starts the vanilla minecraft server."}
stop = { description = "Stops the vanilla minecraft server.", stdin_cmd = "stop" }
restart = { description = "Restarts the vanilla minecraft server."}
[vanilla-mc.bot]
server-id = 0
channel-id = 0
thread-id = 0
user-ids = [ 0, 1 ]
shutdown-msg = "Shuting server down."
